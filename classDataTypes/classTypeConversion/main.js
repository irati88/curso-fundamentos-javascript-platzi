// -- CHECKEO DINÁMICO --
// JavaScript irá corriendo el código y, al llegar a la línea 10, saltará el error

const greeting = "Hola"
console.log(greeting);

const farewell = "Adiós"
console.log(farewell);

KC // Muestra error de sintaxis al llegar a esta parte del código

const description = "porque aprendo mucho"
console.log(description);

// -- CONVERSIÓN DE TIPOS --

const number = 2
const boolean = true;
console.log(number + boolean); // Imprime el 3
